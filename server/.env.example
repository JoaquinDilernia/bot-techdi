# Meta (WhatsApp + Instagram)
META_VERIFY_TOKEN=techdi_webhook_token
META_APP_SECRET=
META_ACCESS_TOKEN=
META_PHONE_NUMBER_ID=
META_IG_PAGE_ID=
META_WHATSAPP_BUSINESS_ACCOUNT_ID=

# Anthropic
ANTHROPIC_API_KEY=

# OpenAI (audio transcription)
OPENAI_API_KEY=

# Firebase Admin SDK
FIREBASE_PROJECT_ID=
FIREBASE_PRIVATE_KEY=
FIREBASE_CLIENT_EMAIL=

# Auth
JWT_SECRET=
# Password for the seeded admin account (joaquin@techdi.com.ar). Set this before first boot.
ADMIN_PASSWORD=

# App
PORT=3001
NODE_ENV=development

# Set to the deployed Vercel dashboard URL(s) (comma-separated) so the backend's
# CORS allowlist accepts requests from it in production (Railway backend + Vercel frontend).
FRONTEND_URL=
